@{
    if (User.Identity.IsAuthenticated && User.IsInRole("Admin"))
    {
        Layout = "~/Views/Shared/_AdminLayout.cshtml"; // ✅ Admins get admin layout
    }
    else
    {
        Layout = "~/Views/Shared/_Layout.cshtml"; // ✅ Regular users get normal layout
    }
}
